#!/bin/perl

use strict;
use warnings;

sub index_interface {
  my %types = %{$_[0]};
 
  for my $t (keys %types) {
    my %i = %{ $types{$t} };



  }

}


sub index_implementation {
  my %types = %{$_[0]};
  
  for my $t (keys %types) {
    my %i = %{ $types{$t} };



  }

}

1;

